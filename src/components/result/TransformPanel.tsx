/**
 * DEPRECATED / UNUSED. The broad AI "Transform Transcript" panel was removed
 * from LinkTranscript (only an optional Summary feature is planned, built
 * separately). This file is a no-op stub kept only because the sandbox could not
 * delete it; it is not imported anywhere. Safe to delete locally.
 */
export function TransformPanel() {
  return null;
}
